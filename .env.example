# ============================================================================
# API AUTHENTICATION (Required for API-only microservice)
# ============================================================================
# Set a strong, random API key to protect your API endpoints
# Generate one using: openssl rand -base64 32
# Or use any secure random string generator
API_SECRET_KEY=your_secure_api_key_here

# Google AI API Key (required for AI-powered scene generation)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=your_google_ai_api_key_here

# Modal.com Token (required for Manim video compilation)
# Get your token from: https://modal.com/settings/tokens
MODAL_TOKEN_SECRET=your_modal_token_here

# Vercel Blob Storage Token (required for video storage)
# Get your token from: https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Context7 API Key (optional - for enhanced documentation context)
CONTEXT7=your_context7_key_here

# Modal Configuration
MODAL_FALLBACK_TO_LOCAL=false
USE_MODAL_COMPILATION=true


# ============================================================================
# STORAGE CONFIGURATION
# ============================================================================
# Choose your storage provider:
# - "vercel-blob": Use Vercel Blob Storage only
# - "s3": Use S3-compatible storage only
# - "auto": Automatically use available provider(s) with fallback (default)
#   In auto mode, if both are configured, Vercel Blob is primary with S3 as fallback
STORAGE_PROVIDER=auto

# ============================================================================
# FILE STORAGE (S3-Compatible)
# ============================================================================
# Required for file uploads (profile photos, voice recordings, documents, etc.)
# Supports AWS S3, DigitalOcean Spaces, and any S3-compatible storage service

# S3 Endpoint URL (MUST include https://)
# For DigitalOcean Spaces: https://{region}.digitaloceanspaces.com
# For AWS S3: Leave empty or use https://s3.{region}.amazonaws.com
# Examples:
#   - DigitalOcean NYC3: https://nyc3.digitaloceanspaces.com
#   - DigitalOcean SGP1: https://sgp1.digitaloceanspaces.com
#   - AWS S3: (leave empty for default)
S3_ENDPOINT=

# S3 Region
# DigitalOcean Spaces: nyc3, sgp1, fra1, sfo3, etc.
# AWS S3: us-east-1, us-west-2, eu-west-1, ap-southeast-1, etc.
S3_REGION=

# S3 Access Credentials
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# S3 Bucket Name
S3_BUCKET=

# Optional: Custom public URL base (e.g., CDN URL)
# If not set, URLs will be generated from endpoint and bucket
# Example: https://cdn.yourdomain.com
S3_PUBLIC_URL_BASE=

# Optional: Force path-style URLs
# Set to true for DigitalOcean Spaces and most S3-compatible services
# Set to false for AWS S3 if using virtual-hosted-style URLs
# Default: true
S3_FORCE_PATH_STYLE=true